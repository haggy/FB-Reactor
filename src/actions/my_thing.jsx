var Constants = require(__dirname + '/../constants'),
    MyThingLib = require('./../libs/my_thing');

/**
* The actions file is where all the magic happens!
* Use the lib module to interact with the DB or API's.
* Don't forget to call dispatch() when you're done!
* this.dispatch() notifies your store that the action is complete.
*/

module.exports = {
    /**
    * Put methods for data processing and other tasks below.
    */
    /**
    performAction: function(data) {
        // Call dispatch() when you're done
        this.dispatch(Constants.MY_EVENT, data);
    }
    */
};
